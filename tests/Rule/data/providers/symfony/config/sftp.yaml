some_package:
    sftp:
        options:
            host: "%foo_ftp_host%"
            username: "%foo_ftp_username%"
            password: "%foo_ftp_password%"
            maxTries: !php/const Symfony\Sftp::RETRY_LIMIT
